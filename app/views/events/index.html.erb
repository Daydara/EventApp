<p id="notice"><%= notice %></p>

<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  </li>
<% end %>

<h1>Events today</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Description</th>
      <th>Date</th>
      <th>Heure</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events_current.each do |event| %>
      <tr>
        <td><%= event.Nom %></td>
        <td><%= event.Description %></td>
        <td><%= event.Date %></td>
        <td><%= event.Heure %></td>

        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>Events comming</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Description</th>
      <th>Date</th>
      <th>Heure</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events_futur.each do |event| %>
      <tr>
        <td><%= event.Nom %></td>
        <td><%= event.Description %></td>
        <td><%= event.Date %></td>
        <td><%= event.Heure %></td>

        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>Events depassed</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Description</th>
      <th>Date</th>
      <th>Heure</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events_depassed.each do |event| %>
      <tr>
        <td><%= event.Nom %></td>
        <td><%= event.Description %></td>
        <td><%= event.Date %></td>
        <td><%= event.Heure %></td>

        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
